botmaster (c2 server)

preliminary work

-dns record hosted on noip.com at localhoneypot.ddns.net
-git credentials stored in ~/.git-credentials
-type of service and coordinates stored in /etc/tor/torrc (require a httpserver)
-telegram bot created via botfather and added as admin in a public channel
-solcx for compile solidity contracts for ethereum blockchain using python
-web3py to interact with ethereum blockchain using python
-infura api to get a testnet for ethereum
-ethereum wallet needed to do transactions (metamask used)
-openvpn server up (and bot user added in config)
-ssh client
